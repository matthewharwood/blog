<p id="notice"><%= notice %></p>
<div class="modal-image">
	<%= image_tag @work.avatar.url(:medium), :class => "featured full-bleed" %>
</div>
<p>
  <b>Name:</b>
  <%= @work.name %>
</p>

<p>
  <b>Subtitle:</b>
  <%= @work.subtitle %>
</p>


<%= link_to 'Edit', edit_work_path(@work) %> |
<%= link_to 'Back', works_path %>
